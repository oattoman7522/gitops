---
# Source: mychart/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  labels:
    app: prd-app
  name: prd-app-service
  namespace: prd-app-ns
spec:
  ports:
  - name: http
    nodePort: 30680
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: prd-app
  type: NodePort
